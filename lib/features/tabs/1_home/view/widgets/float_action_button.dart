import 'package:flutter/material.dart';

FloatingActionButton floatingActionButton({required IconData? icon}) {
  return FloatingActionButton(
    onPressed: () {},
    child: Icon(icon),
    backgroundColor: Colors.white,
  );
}
